<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Book Notes</title>
  </head>
  <body>
    <div class="header">
      <h1>Book Notes - by Adam Shay</h1>
      <h2>Books I've Read</h2>
      <p>
        Tiny summary but detailed notes for each. Use the ISBN number to find it from your local library or anywhere
        else. This page will constantly update as I read more, so bookmark it if you want to check back in a few months.
      </p>
      <form class="sorting" action="/sort" method="get">
        <p>Sorted with my top recommendations up top. Sort by</p>
        <button type="submit" name="sort" value="title">Title</button>
        <button type="submit" name="sort" value="date">Newest</button>
        <button type="submit" name="sort" value="rating">Best</button>
      </form>
    </div>

    <div class="book-list">
      <% books.forEach(function(book){%>
      <div class="book-item">
        <img src="<%= book.cover %>" value="<%= book.id %>" />
        <h3 class="book-title" value="<%= book.id %>"><%= book.title %></h3>
        <p class="book-description"><%= book.description %></p>
      </div>
      <%}); %>
    </div>
  </body>
</html>
